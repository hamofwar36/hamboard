{
  "name": "hamboard-mobile-web",
  "private": true,
  "version": "0.3.31",
  "description": "Hamboard mobile web app deployment package",
  "scripts": {
    "mobile:build": "node scripts/prepare-mobile-deploy.mjs",
    "mobile:preview": "npm run mobile:build && node scripts/serve.mjs dist-mobile",
    "qa:mobile-deploy": "node scripts/validate-mobile-deploy.mjs"
  }
}
